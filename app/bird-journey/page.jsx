import Home from '../page';

export const metadata = { title: 'Halevora — Hummingbird journey preview', robots: { index: false, follow: false } };

export default function BirdJourneyPreview() {
  return <Home />;
}
